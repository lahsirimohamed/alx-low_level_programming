#include <stdio.h>
#include "main.h"
/**
 *_abs - omputes the absolute value of an integer
 *@n: the number to check
 *Return: the abs value of n
 */
int _abs(int n)
{
if (n >= 0)
return (n);
else
return (-n);
}
