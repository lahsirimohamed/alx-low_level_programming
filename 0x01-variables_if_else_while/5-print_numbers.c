#include <stdio.h>
/**
 *main - entry point of our prg
 *Return: 0 if success
 */
int main(void)
{
int number;
for (number = 0; number < 10; number++)
{
printf("%d", number);
}
printf("\n");
return (0);
}
